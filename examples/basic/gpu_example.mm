/**
 * @file gpu_example.mm
 * @brief GPU example compiled as Objective-C++ to support Metal
 * 
 * This is the same as gpu_example.cpp but compiled as Objective-C++
 * to enable full Metal support on macOS.
 */

// Include the C++ example
#include "gpu_example.cpp"